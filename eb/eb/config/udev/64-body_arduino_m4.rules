# rules for Arduino Feather M4 Express in EB Body
# WARNING - no serial number, so if second M4 is added, "KERNELS" must be used (see readme):  KERNELS=="xxxxx"

SUBSYSTEM=="tty", ENV{ID_VENDOR_ID}=="239a", ENV{ID_MODEL_ID}=="8022", KERNELS=="1-2.2:1.0", MODE:="0666", GROUP:="dialout", SYMLINK+="body_arduino"
ATTRS{idVendor}=="239a", ENV{ID_MM_DEVICE_IGNORE}="1"



